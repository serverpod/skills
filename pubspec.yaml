name: skills
description: >
  CLI that installs AI agent skills from your Dart and Flutter package
  dependencies into your IDE, so Antigravity, Claude, Codex, Cursor, and other
  assistants know how to use your stack.
version: 0.4.0-dev
repository: https://github.com/serverpod/skills
issue_tracker: https://github.com/serverpod/skills/issues

executables:
  skills:

environment:
  sdk: ^3.5.0

dependencies:
  args: ^2.7.0
  cli_util: ^0.5.1
  config: ^0.8.0
  glob: ^2.1.0
  http: ^1.6.0
  io: ^1.0.5
  logging: ^1.3.0
  meta: ^1.18.2
  package_config: ^2.1.0
  path: ^1.9.0
  yaml: ^3.1.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
  test_descriptor: ^2.0.0
